# 🍎 水果證交所 PRO (Fruit Stock Exchange System)

這是一個結合 **AI 生成內容** 與 **即時模擬市場** 的股票交易遊戲系統。
系統透過複雜的演算法模擬真實市場的波動，並利用 Google Gemini AI 動態生成財經新聞、專家預測與新公司 IPO。

---

## 🛠️ 技術架構 (Tech Stack)

### Backend (後端)
- **Framework**: Python FastAPI (Async)
- **Database**: PostgreSQL (SQLModel ORM)
- **Cache / IPC**: Redis (用於 Pub/Sub 即時行情廣播)
- **AI**: Google Gemini Pro/Flash API
- **Scheduler**: APScheduler (處理週一 IPO、定時任務)

### Frontend (前端)
- **Framework**: React 18 (Vite)
- **UI Lib**: TailwindCSS, Shadcn/UI, Lucide Icons
- **Charts**: Lightweight-Charts (TradingView)
- **State**: Context API + WebSocket (useSocket)

### DevOps
- **Docker**: 完整容器化部署 (Backend, Frontend-Nginx, Postgres, Redis)
- **Nginx**: 反向代理與靜態資源服務

---

## ⚙️ 核心機制詳解 (Core Mechanics)

### 1. 📈 市場引擎 (Market Engine)

### 1. 📈 市場引擎 (Market Engine Mechanics)

系統透過物理模擬計算每秒的價格變動，公式如下：

> `Price Change % = Noise + Trend + Sniper + Guru + Gravity`

#### A. 基礎波動 (Base Volatility)
不同板塊有不同的風險係數：
*   **🍎 水果股 (Fruit)**: 基準波動 **1.0x** (標準)
*   **🥩 肉類股 (Meat)**: 基準波動 **1.2x** (高風險)
*   **🥔 根莖股 (Root)**: 基準波動 **0.3x** (超穩定，主要領股利)

#### B. 新聞衝擊與混沌 (Event Impact & Chaos)
*   **利多/利空**：新聞會給予股價 `±0.05 ~ ±0.3` 的動量。
*   **混沌模式 (Chaos Mode)**：當短時間內累積衝擊值 `> 0.15` (例如連續發生重大新聞)，市場進入恐慌狀態，**波動率瞬間放大 3 倍**。

#### C. 地心引力 (Gravity / Mean Reversion)
為了防止股價無限上漲或歸零，系統設有回歸機制：
*   **過熱 (Overvalued)**：當股價 > 基值的 **1.5倍**，引力係數 `-0.001` (拉回)。
*   **過冷 (Undervalued)**：當股價 < 基值的 **0.7倍**，反彈係數 `+0.001` (抄底)。
*   **根莖類特權**：根莖股擁有 **5倍** 的強力引力，強制股價維持在基值附近，確保配息殖利率穩定。

#### D. 專家與狙擊手 (Guru & Sniper)
*   **專家漂移**：每位專家的預測會給予 `±0.00005` 的微幅推力。
*   **狙擊手 (Sniper)**：**0.02%** 機率觸發「胖手指」，瞬間造成 `±2%` 的跳漲或是閃崩。

### 2. 🤖 AI 專家預測系統 (Guru System)

*   **五大性格專家**：
    *   🐺 **華爾街之狼** (激進多頭)
    *   🔮 **水晶球婆婆** (玄學空頭)
    *   🤖 **AI 量化機器人** (數據流)
    *   👴 **穩健老王** (保守派)
    *   🔗 **區塊鏈信仰者** (科技狂熱)
*   **自我實現預言 (Self-Fulfilling Prophecy)**：
    *   當專家發布預測 (Bull/Bear) 時，會產生微幅的「專家漂移 (Guru Drift)」力道，推動股價往預測方向移動。
    *   **命中獎勵**：若股價真的達到目標價，AI 會生成「專家神準預言」的新聞，並對股價造成二次衝擊。

### 3. 📉 破產與重組 (Bankruptcy & Rebrand)

*   **觸發條件**：當股價低於 **$0.05** 時，公司宣告破產。
*   **後果**：
    *   所有持有該股票的玩家，持倉歸零 (壁紙)。
    *   **AI 重組**：系統立即呼叫 Gemini AI，生成一家全新的公司 (名稱、代號)，並在原價位附近重新上市 (Re-IPO)。

### 4. 🎉 新股上市 (Weekly IPO)

*   **觸發時間**：每週一早上 9:00。
*   **機率**：5% 機率觸發。
*   **流程**：AI 生成一家全新公司，給予較高的初始波動率，並發布「新股上市」公告，吸引資金炒作。

### 5. 💸 股利分紅 (Dividends)

*   **機制**：每 10 分鐘（遊戲時間）結算一次。
*   **計算**：根據公司盈利能力隨機發放股利 (`Dividend Yield` 約 1% ~ 5%)。
*   **被動收入**：持有股票的玩家會自動收到現金入帳，鼓勵長期持有優質股。

### 6. 🏇 皇家賽馬場 (Royal Racecourse)

這是一個高風險高回報的博弈系統，讓資金有快速流動的出口。

*   **比賽頻率**：每 2 分鐘一場。
*   **勝率與賠率 (Odds & Win Rate)**：
    *   **公平賠率**：系統移除了莊家抽水，賠率直接反映勝率倒數 (例如勝率 50% = 賠率 2.0)。這使得賠率看起來非常有吸引力，誘使玩家下注。
    *   **殺大賠小 (Kill Logic / 殺豬盤)**：
        *   系統內建「營收保護機制」。
        *   當某匹馬的總下注金額超過彩池的 **20%** 時，該馬會受到「大熱門懲罰」，勝率顯著降低。
        *   **結果**：大眾看好的熱門馬往往會爆冷落馬，而無人問津的冷門馬則經常勝出，讓系統能有效回收玩家資金。
*   **即時動畫**：前端使用 Easing 函數與有機噪聲 (Organic Noise) 模擬真實賽馬動態，冠軍衝線時會有戲劇性的超車效果。

---

## 💰 玩家與交易系統

### 1. 資產計算
*   **淨值 (Net Worth)** = 現金 (Balance) + 股票市值 (Stock Value)。
*   **即時更新**：前端透過 WebSocket 接收即時報價，動態計算總資產，無需頻繁刷新 API。

### 2. 交易限制
*   **買入**：餘額不足時回傳 200 (附帶 Error Status)，前端顯示紅色警告。
*   **賣出**：持股不足時回傳 200 (附帶 Error Status)，前端顯示紅色警告。
*   **T+0**：目前設定為即時交割，買入後可立即賣出。

### 3. 用戶體驗優化
*   **註冊/登入**：
    *   嘗試註冊已存在帳號 -> 引導去登入 (200 OK)。
    *   嘗試登入不存在帳號 -> 引導去註冊 (200 OK)。
    *   密碼錯誤 -> 友善提示 (200 OK)。
*   **跑馬燈 (Ticker)**：
    *   紅燈🔴：系統斷線中。
    *   綠燈🟢：系統正常。
    *   黃字⚠️：重大市場快訊。
    *   **通知防重 (Deduplication)**：前端實作了 `useRef` 紀錄最後一次事件 ID，防止因 WebSocket 頻繁更新導致同一則新聞重複彈出 Toast 通知。

---

## 🚀 部署與執行

### 環境變數 (.env)
```ini
GEMINI_API_KEY=your_key_here
DATABASE_URL=postgresql://user:password@postgres:5432/stock_sim
REDIS_URL=redis://redis:6379/0
```

### Docker 啟動 (推薦)
```bash
# 包含 DB, Redis, Backend, Frontend
docker-compose up --build
```
- Frontend: `http://localhost:3002` (或 NAS IP:3002)
- Backend API: 內部串接，外部不可見 (透過 Nginx Proxy `/api`)

### 🔄 如何更新程式碼 (不影響資料庫)
由於資料庫使用 Docker Volume (`postgres_data`) 掛載，**重建容器並不會清除資料**。
若您修改了前端或後端程式碼，只需再次執行：
```bash
docker-compose up --build -d
```
系統會自動：
1. **保留** 資料庫資料 (股票、用戶資產)。
2. **重建** 後端與前端鏡像 (套用新程式碼)。
3. **重啟** 服務。

若只要更新特定服務 (例如只改了前端)：
```bash
docker-compose up --build -d frontend
```

### 本地開發 (Local Dev)
```bash
# Backend
cd backend
uv run uvicorn main:app --reload --port 8080

# Frontend
cd frontend
npm run dev
# Access: http://localhost:3002
```

---

## 📂 目錄結構
- `backend/market.py`: 市場核心物理引擎。
- `backend/ai_service.py`: Gemini AI 串接邏輯。
- `backend/trader.py`: 交易搓合邏輯。
- `frontend/src/context/SocketContext.jsx`: WebSocket 即時連線管理。
- `frontend/src/components/charts/`: K線圖組件 (Wrapper for Lightweight-charts).
